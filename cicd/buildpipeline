pipeline {
    agent any
    tools {
      maven 'mymaven'
      }
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean package'
            }
        }
        stage('Deploy') {
            steps {
                sshagent(['mvnuser_jenk']) {
    // scp <src_dir> user@tomcatserverip:/tomcat/webapps
    sh "scp -o StrictHostKeyChecking=no target/*.war mvnuser@52.90.174.106:/home/mvnuser/tomcat/webapps"
}
            }
        }
    }
}
